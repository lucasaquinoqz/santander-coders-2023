import { RedIfTodayPassesDirective } from './red-if-today-passes.directive';

describe('RedIfTodayPassesDirective', () => {
  it('should create an instance', () => {
    const directive = new RedIfTodayPassesDirective();
    expect(directive).toBeTruthy();
  });
});
